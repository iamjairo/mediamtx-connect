{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the tray app's windows.",
  "windows": ["settings", "logs"],
  "permissions": [
    "core:default",
    "store:default"
  ]
}
